 python3 -c "from sqlalchemy import create_engine; from models import Base; engine = create_engine('postgresql://momu_user:superparol2026@momu_postgres:5432/momu_test'); Base.metadata.create_all(engine); print('Uspeh')"

